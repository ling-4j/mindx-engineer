apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: week1
  labels:
    app: api
    environment: production
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
  - name: http
    port: 3000
    targetPort: http
    protocol: TCP
